// Task12.jsx - Promise Middleware (basic)
const Task12 = () => (
  <div>
    <h2>Task 12: Promise Middleware</h2>
    <p>Create middleware that checks if <code>action.payload.then</code> is a promise, then dispatches on resolve/reject.</p>
  </div>
);

export default Task12;