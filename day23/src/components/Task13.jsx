// Task13.jsx - Combine Middleware
const Task13 = () => (
  <div>
    <h2>Task 13: Combine Middleware</h2>
    <p>Use <code>applyMiddleware(thunk, logger)</code> to combine Redux Thunk and custom logger middleware in <code>store.js</code>.</p>
  </div>
);

export default Task13;